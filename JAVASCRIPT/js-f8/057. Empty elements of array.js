// forEach, find, filter, reduce, some, every

var courses = [
    'Javascript',
    'Java',
    'PHP',
    'Python'
];

courses.length = 10;
for (var index in courses) {
    console.log(courses[index]);
}

